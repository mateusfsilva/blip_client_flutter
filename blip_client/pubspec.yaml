name: blip_client
description: A new flutter plugin project.
author: Mateus Freitas (mateus@blubybs2.com)
repository: https://github.com/mateusfsilva
issue_tracker: https://github.com/mateusfsilva/<repository>/issues
version: 0.0.1

dependencies:
  flutter:
    sdk: flutter
  
  blip_client_platform_interface:
    git:
      url: https://github.com/mateusfsilva/blip_client_flutter.git
      ref: 59f92d89bfa61c6f2f5883a2d6289a25b7242279
      path: blip_client_platform_interface

dev_dependencies:
  flutter_test:
    sdk: flutter

  effective_dart: ^1.3.0

environment:
  sdk: ">=2.10.4 <3.0.0"
  flutter: ">=1.22.5"

flutter:
  plugin:
    platforms:
      android:
        package: com.blubybs2.blip_client
        pluginClass: BlipClientPlugin
      ios:
        pluginClass: BlipClientPlugin
